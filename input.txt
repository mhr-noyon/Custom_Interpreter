main:
     int x, y;
     scan x, y;
     if x > y:
          int i;
          print "Max: ", x;   
     elif y > x:       
          print "Max: ", y;
     else:
          print "Equal";
     endif
     loop i = 1: i<=5: i=i+1:
          if i<3:
               print i;
          else:
               break;
          endif
     endloop
endmain
